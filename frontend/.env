
# Frontend dev server port (set to 5173 to avoid conflict with Rails on 3000)
PORT=5173

# Point the frontend at the Rails API (use 127.0.0.1 so origins match exactly)
REACT_APP_API_URL=http://127.0.0.1:3000/api/v1

